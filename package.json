{
  "name": "log-parser",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@blueprintjs/core": "^3.35.0",
    "@loadable/component": "^5.14.1",
    "gatsby": "latest",
    "gatsby-plugin-react-helmet": "latest",
    "js-untar": "^2.0.0",
    "jszip": "^3.5.0",
    "normalize": "^0.3.1",
    "pako": "^1.0.11",
    "react": "latest",
    "react-dom": "latest",
    "react-helmet": "latest",
    "react-lazylog": "^4.5.3"
  },
  "scripts": {
    "develop": "gatsby develop",
    "deploy": "gatsby build --prefix-paths && echo 'logparser.alexdykstra.com' > public/CNAME && gh-pages -d public",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "post-update": "echo \"codesandbox preview only, need an update\" && yarn upgrade --latest"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
